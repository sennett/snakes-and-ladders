describe('Game', () => {
  describe('exposing the game state', () => {
    it.todo('includes the board state')

    describe('including the players', () => {
      it.todo('lists all the players by name')
      it.todo('shows the player who\'s current turn it is')
    })
  })
  describe('takeTurn()', () => {
    it.todo('cycles through the players')
    it.todo('detects the end state and finishes the game')
  })
})

export const whatever = ''
