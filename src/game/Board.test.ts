import Board from './Board'

describe('Board', () => {
  it('can be created', () => {
    let board = new Board()
    expect(board).toBeInstanceOf(Board)
  })

  it('takes the pieces', () => {
    let board = new Board()
    expect(board).toBeInstanceOf(Board)
  })
})